{
  "name": "zyra2d-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "Zyra2D - High-performance 2D game engine monorepo",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -C examples/basic-demo dev",
    "test": "echo \"no tests yet\"",
    "lint": "echo \"no lint yet\""
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
