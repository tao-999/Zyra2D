{
  "name": "zrya-root",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r --filter ./packages/** run build",
    "dev": "pnpm -r --filter ./packages/** run dev",
    "test": "pnpm -r --filter ./packages/** run test || exit 0",
    "lint": "pnpm -r --filter ./packages/** run lint || exit 0",
    "clean": "pnpm -r --filter ./packages/** run clean || exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
